#include <stdio.h>
#include <stdlib.h>

int main()
{
    for (/*Inicializacion*/; /*Condicion*/; /*Accion Posterior*/){
        /*La condicion es la unica obligatoria pero la accion posterior comunmente es necesaria
        para que el for termine, aunque puede ser que se realice esa accion dentro del for.
        La inicializacion no es obligatoria

        Apenas comienza el for, se ejecuta la inicializacion y se evalua la condcion. AL final de esa vuelta y de las demas vueltas, se realizan las acciones posteriores, se evalua
        la condicion y entra al for si se cumple
        */
    }
    return 0;
}
